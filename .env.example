# Copy this file to .env and fill in values. Never commit .env.
# The static site does not use any server-side environment variables.
# If deploying with a serverless backend later, add API keys here.
